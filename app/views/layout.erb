<!DOCTYPE html>

<html>

	<head>
	
			<title>Simon Says....</title>
	  <link rel="stylesheet" type="text/css" href="/css/application.css">  
	</head>

	<body>

		<% if current_user %>
			Welcome, <%= current_user.name %>
		<% end %>

		<% if flash[:errors] && ![:errors].empty? %>
			Sorry, there were the following problems with your information.
			<ul id="errors">
				<% flash[:errors].each do |error| %>
					<li><%= error %></li>
				<% end %>
			</ul>
		<% end %>

		<%= yield %>
	
	</body>
	
</html>